#include<stdio.h>
#include<string.h>

void main()
{
	char text[50],pat[50];
	
	printf("Enter the Text :");
	scanf("%s", &text);
	
	printf("Enter the Pattern :");
	scanf("%s", &pat);
	
}

void rabin( char text[], char pat[], int q )
{
	int N = strlen(text);
	int M = strlen(pat);
	int i,j,t=0,p=0,h;
	
	h=pow(d,M-1)%q;
	for(i=0; i<M-1; i++)
	{
		
	
	
	
